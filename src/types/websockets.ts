export enum EProductMessage {
  client = 'clientSendProduct',
  server = 'serverSendProducts',
}

export enum EChatMessage {
  client = 'clientSendChatMessage',
  server = 'serverSendChatMessages',
}
